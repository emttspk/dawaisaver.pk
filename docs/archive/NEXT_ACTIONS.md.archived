# Next Actions

## Current Task

DawaiSaver Hetzner DRAP Mirror Resume Phase.

## Completed

- Retired active legacy runtime references from the deployment and mirror execution path.
- Locked DRAP mirror execution behind `MIRROR_ENABLED=false` and `MIRROR_MIGRATION_MODE=true`.
- Updated deployment notes for Hetzner, Coolify, PostgreSQL 18, Cloudflare R2, and Cloudflare DNS.
- Added `COOLIFY_APP_DEPLOYMENT.md`, `COOLIFY_ENV_TEMPLATE.md`, and the migration checklist.
- Passed `npm run prisma:generate`.
- Passed `npm run build`.
- Passed `npm test -- --runInBand`.
- Verified the mirror route surface and checkpoint-resume code path in source.
- Verified the therapeutic-category flag now uses `is_primary` in migrations and Prisma.

## Next

1. Resume the DRAP mirror from the stored checkpoint.
2. Verify the live mirror endpoints respond with authenticated admin access.
3. Confirm counters increase and record the active batch ID.
4. Keep mirror execution paused again only if a new blocker is discovered.

## Exact Next Prompt

Resume the DRAP mirror from the existing checkpoint in production, verify the counters increase, and report the active batch ID and progress totals.

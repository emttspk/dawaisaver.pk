# Seed Structure

## Purpose

This directory is reserved for seed datasets and seed plans.

## Initial Policy

Only verified, low-risk reference data should be seeded. Production medicine, price, prescription, bill, and crawl data must enter through auditable source adapters.

## Future Seed Files

- `roles.seed.ts`
- `source-types.seed.ts`
- `normalization-rules.seed.ts`
- `dosage-forms.seed.ts`
- `strength-units.seed.ts`

